# Stock News Ticker Plugin Requirements

# Core dependencies (handled by main LEDMatrix)
# requests - for RSS feed and API calls
# Pillow - for image manipulation

# These are already included in the main LEDMatrix requirements
# but listed here for reference:
# requests>=2.25.0
# Pillow>=8.0.0

# Safe XML parsing (prevents XML entity expansion attacks on untrusted RSS feeds)
defusedxml>=0.7.1
